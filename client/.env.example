# Leave VITE_API_URL empty in development — the Vite proxy in
# vite.config.js forwards /api to http://localhost:5000.
#
# In production point it at the deployed API, e.g.
# VITE_API_URL=https://api.charubalainc.com
VITE_API_URL=
